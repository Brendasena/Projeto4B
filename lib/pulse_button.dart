import 'dart:html';

import 'package:flutter/material.dart';

class PulseButton extends StatelessWidget {
  const PulseButton({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text(''),
      ),
      body: Container(),
    );
  }
}
